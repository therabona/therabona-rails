.nested-fields
  = image_tag f.object.file.thumb.url, class: "pull-left" if f.object.file.thumb.url
  = f.file_field :file
  = link_to_remove_association icon("minus", "Remove Image"), f, class: "btn btn-link btn-xs"